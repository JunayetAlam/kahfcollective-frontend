export type Tier = {
  id: string;
  name: string;
  price: number;
  points: string[];
  isHide: boolean;
  isMostPopular: boolean;
  isDeleted: boolean;
  stripePriceId: string;
  stripeProductId: string;
  createdAt: string;
  updatedAt: string;
};
